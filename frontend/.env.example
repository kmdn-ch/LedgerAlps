# LedgerAlps Frontend — Vite environment variables
# Copy to .env.local for local development

# Go backend (default for local dev — no proxy needed)
VITE_API_URL=http://localhost:8000/api/v1

# Python backend (if still using the old FastAPI backend)
# VITE_API_URL=http://localhost:8000/api/v1
